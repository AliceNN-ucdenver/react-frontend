/**
 * Admin movie list page — stub.
 * Full implementation pending.
 */
export function AdminMovieListPage() {
  return (
    <div>
      <h1>Movies</h1>
      <p style={{ color: '#666' }}>Admin feature — implementation pending.</p>
    </div>
  );
}
