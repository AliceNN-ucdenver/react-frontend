/**
 * Admin actor list page — stub.
 * Full implementation pending.
 */
export function AdminActorListPage() {
  return (
    <div>
      <h1>Actors</h1>
      <p style={{ color: '#666' }}>Admin feature — implementation pending.</p>
    </div>
  );
}
